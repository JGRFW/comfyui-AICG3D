## 渲染器跑起来就能看到画面：实时预览 + 百分比进度条

**以前**：`AICG-渲染器（高级）` 跑起来只有一个绿框。采样要好几分钟，不知道跑到第几步，也不知道画面对不对，只能等出片。

**现在**：节点底部多了一块预览区 + 一条进度条。

- 采样每一步刷新一次预览图（该步的 x0 潜空间预览），构图不对可以立刻中断，不用白等。
- 进度条带百分比、已用时间与剩余估计；解码 / 合成阶段没有可拆分的步数，用来回跑的亮条表示还在动。
- 说明：ComfyUI 自带的 latent 预览只画在 Vue 节点模式上，经典画布模式下这个节点一直看不到画面，所以这条通道是本插件自己实现的（新增 `h3easy/render_progress.py`）。
- 默认用 latent RGB 因子出图，看构图足够；想要更接近成片，把 **设置 → 执行 → 预览方式** 改成 `taesd`（需要 `models/vae_approx/` 里有 `taeh3` 权重）。
- 预览显示的是视频 latent 的首帧；节点底部会固定留出预览区高度，所以节点比原来高一点。

## 加载器 LoRA 槽位 4 → 9

面板右下角的 `− 1 +` 现在最多能加到 `9 / 9`。

新增的槽位统一排在 `非 H3 权重处理` 之后追加，前面 14 个 widget 的位置一个没动 —— **旧工作流里的 LoRA 不会串位，不用重新接线**。

## 提示词方案：官方 8 个风格开放中文 + 新增 5 个中文场景

- 官方 8 个风格方案（3D 动画短片 / 品牌宣传片 / 合作游戏开场 / 手绘实拍融合 / 极简产品广告 / 音乐视频字幕 / 纸张拼贴解说 / 纸艺定格解说）的正文是官方英文原文，这次把它们的可选语言放开到中文模式，中文界面下也能直接选。
- 新增 5 个中文场景方案：**东方巨构导演、真实手机自拍 Vlog、光感小清新短片、超现实电影底图导演、15 秒剧情镜头导演**。

## 非 H3 权重不再让整条链路报错

模型槽位里选到非 H3 权重（例如 Anima 这类图像模型）时：

- **自动回落**（默认）：忽略它，改用另一条 H3 权重照常出片，控制台会说明实际用了哪条；
- **按所选加载**：严格按你选的加载，不是 H3 就直接报错，方便排查；
- 已经探测过的权重会缓存，不会每次采样都重复读取。

## 其他修复与调整

- 采样 seed 支持完整 64 位，修掉 `roll_inference_rng` 缺失导致的崩溃。
- 与上游 ComfyUI-MiniMaxH3-Easy 同名撞车时，前端不再把上游那套节点改名 / 改分类 / 改配色。
- 界面：提示词输入框高度加到 4 倍、参考尺寸改成像素显示、去掉提示词框右下角的素材库按钮、工具栏图标放大。
- 目录整理：`skills/` 下与 `prompt_guides/` 重复的 4 套技能包已删除（内容与引用路径都在 `prompt_guides/`）；`workflows/1-7` 旧示例工作流已整理进 `workflows/upstream/`。

---

安装：把仓库放进 `ComfyUI/custom_nodes/comfyui-AICG3D`，重启 ComfyUI，然后 `Ctrl+F5` 强刷一次浏览器缓存。